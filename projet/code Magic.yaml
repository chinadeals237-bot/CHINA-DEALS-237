workflows:
  build_app:
    name: CHINA DEALS Build
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Get dependencies
        script: flutter pub get
      - name: Build Android
        script: flutter build apk
       --release --no-codesign
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
      